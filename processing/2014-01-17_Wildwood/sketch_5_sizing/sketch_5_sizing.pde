void setup() {
  size(400, 400);
  //size(600, 800);
  
  stroke(255);
}

void draw() {
  background(0, 64, 128);
  
  line(200, 200, mouseX, mouseY);
  //line(width/2, height/2, mouseX, mouseY);
  
}

